# {{project_name}}

This project was generated by `apmpr init`. Its deployment starts empty so you can
declare the repositories, source worktrees, startup profiles, instances, and groups that
belong to this project.

Validate and inspect the project before starting it:

```sh
apmpr validate deployment.yaml
apmpr plan deployment.yaml
apmpr up deployment.yaml
apmpr status deployment.yaml
apmpr down deployment.yaml
```

Edit `deployment.yaml` to add repositories once, then add ordinary worktrees under
`sources:` as `{ repository, ref, path }`. `apmpr up` creates missing repository
stores and source worktrees before starting instances. Runtime and generated state is
kept under `.apmpr/`.

Codex-compatible agents can use the project-local `$apmpr-project` skill under
`.agents/skills/` for the safe edit, validation, lifecycle, and recovery workflow.
